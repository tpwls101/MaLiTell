import { useState } from "react";
import * as s from "../../../styles/auth/profile/myReservation";

export interface myReviewData {

}

export default function MyReview() {
  // const [myReviews, setMyReviews] = useState<mySHGroupData[]>();
  // useEffect(() => {
  //   mySHGroup().then((res) => {
  //     console.log(res);
  //     setMyGroups(res);
  //   });
  // }, []);
  return <s.Wrapper>내 리뷰</s.Wrapper>;
}
